/*Write a program to input two numbers and print the result of:
(a + b) * (a - b)*/
#include<iostream>
using namespace std;
int main(){
    float a,b;
    cout<<"Enter two number: ";
    cin>>a>>b;
    cout<<"The result of (a + b) * (a - b)is "<<(a + b) * (a - b);
    return 0;
}
